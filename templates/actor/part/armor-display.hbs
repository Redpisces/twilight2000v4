{{log this}}
<div class="grid-item flexcol" style="grid-column-end:span 1;">
  <div class="header_row flexrow"><span>Armor<span></div>
  <div class="flexrow">
    {{#each this.armorValue as |value id|}}
    <span>{{id}}</span>
    {{/each}}
  </div>
  <div class="flexrow">
    {{#each this.armorValue as |value id|}}
    <span>{{value}}</span>
    {{/each}}
  </div>
</div>
